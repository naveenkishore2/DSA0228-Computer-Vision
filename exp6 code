#Exp 6. Image Translation (Moving Image)

import cv2
import numpy as np
from google.colab.patches import cv2_imshow

img = cv2.imread("flower_image.png")

rows, cols = img.shape[:2]

M = np.float32([[1,0,100],[0,1,50]])

moved = cv2.warpAffine(img, M, (cols, rows))

cv2_imshow(moved)
